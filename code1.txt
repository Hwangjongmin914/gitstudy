sososo
mememe
git init, git add, git commit -m "????", git diff
git shortlog: commit change log
git pull gogo
git reset (id)HEAD~1 // 뒤로 돌아간다~ --soft, --mixed, --hard(수정한거 다날라감)
git revert (id)HEAD~? // 뒤로 돌아가는데 새로운 커밋만들면서
git branch
git merge development // 가져오고
git add.
git commit -am "합체!!"
